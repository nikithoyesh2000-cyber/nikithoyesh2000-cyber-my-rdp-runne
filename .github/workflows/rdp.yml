name: Windows RDP Runner

on:
  repository_dispatch:
    types: [start-rdp]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Enable RDP, Services & Firewall
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0
          
          Set-Service -Name "TermService" -StartupType Automatic
          Start-Service -Name "TermService" -ErrorAction SilentlyContinue
          Set-Service -Name "UmRdpService" -StartupType Automatic
          Start-Service -Name "UmRdpService" -ErrorAction SilentlyContinue
          
          New-NetFirewallRule -DisplayName "Allow RDP Port 3389" -Direction Inbound -LocalPort 3389 -Protocol TCP -Action Allow -ErrorAction SilentlyContinue
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop" -ErrorAction SilentlyContinue

      - name: Create RDP User
        run: |
          net user RDPUser "Pass2026RDP#!" /add
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "RDPUser"
          Add-LocalGroupMember -Group "Administrators" -Member "RDPUser"

      - name: Install & Start Localtonnel
        run: |
          npm install -g localtunnel
          
          # Start localtunnel on background
          Start-Process -FilePath "npx.cmd" -ArgumentList "localtunnel --port 3389" -RedirectStandardOutput "lt.log" -RedirectStandardError "lt_err.log" -NoNewWindow
          
          Start-Sleep -Seconds 10

          if (Test-Path "lt.log") {
              $logs = Get-Content "lt.log"
              Write-Host "--- Localtonnel Logs ---"
              Write-Host ($logs -join "`n")
              
              $urlMatch = $logs | Select-String -Pattern 'https://(.*)'
              if ($urlMatch) {
                  $RdpAddress = $urlMatch.Matches[0].Groups[1].Value
                  Write-Host "============================================="
                  Write-Host "🚀 RDP Address : $RdpAddress"
                  Write-Host "👤 Username    : RDPUser"
                  Write-Host "🔑 Password    : Pass2026RDP#!"
                  Write-Host "============================================="
              }
          }

      - name: Keep Runner Alive
        run: |
          $i = 0
          while ($true) {
            Start-Sleep -Seconds 60
            $i++
            Write-Host "RDP Active... ($i mins elapsed)"
          }
